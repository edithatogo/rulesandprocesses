# Technology Stack

The extraction track will choose the smallest stack that can validate released
PIC JSON Schema contracts and source manifests deterministically. Until that
decision is implemented:

- JSON and JSON Schema 2020-12 are the expected interchange formats.
- Markdown records source, method, governance, and representational limits.
- Adapter-specific runtimes remain isolated and optional.
- No dependency or runtime is implied by this scaffold.
